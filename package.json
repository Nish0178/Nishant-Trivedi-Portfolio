{
  "name": "nishant-trivedi-portfolio",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint",
    "backend:test": "cd backend && (mvnw.cmd test || mvn test)",
    "backend:run": "cd backend && (mvnw.cmd spring-boot:run || mvn spring-boot:run)"
  }
}
